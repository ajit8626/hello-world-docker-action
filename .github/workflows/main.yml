on:
  push:
    
jobs:
  hello_world:
    runs-on: ubuntu-latest
    name: to say hello
    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: docker build
        run:
          docker build -f Dockerfile .